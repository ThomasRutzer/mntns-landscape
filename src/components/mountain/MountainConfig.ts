const config = {
    parameters: {
        height: {
            min: 10,
            max: 100,
            default: 50
        },

        thickness: {
            min: 5,
            max: 20,
            default: 10
        }
    },
};

export default config;
